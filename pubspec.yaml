name: notifications_flutter
description: "A new Flutter Notifications Package."
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=1.17.0"

dependencies:
  cloud_firestore: ^6.6.0
  core_flutter:
    git:
      url: git@github.com:lenblazy/core_flutter.git
  equatable: ^2.0.7
  firebase_core: ^4.11.0
  firebase_messaging: ^16.4.1
  flutter:
    sdk: flutter
  flutter_local_notifications: ^21.0.0
  fluttertoast: ^9.0.0
  get_it:
  injectable:

dev_dependencies:
  build_runner:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  injectable_generator:
  mocktail:

flutter:
